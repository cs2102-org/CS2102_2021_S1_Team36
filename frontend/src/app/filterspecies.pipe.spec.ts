import { FilterspeciesPipe } from './filterspecies.pipe';

describe('FilterspeciesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterspeciesPipe();
    expect(pipe).toBeTruthy();
  });
});
